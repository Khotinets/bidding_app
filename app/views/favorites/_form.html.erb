<%= form_with(model: favorite, local: true) do |form| %>
  <% if favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user %>
    <%= form.text_field :user, id: :favorite_user %>
  </div>

  <div class="field">
    <%= form.label :product %>
    <%= form.text_field :product, id: :favorite_product %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
